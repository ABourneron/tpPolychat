# TP WEB tchat

Le Polychat à été réalisé dans le cadre des TP de Web. Le tchat repose sur Socket.IO.

## Installation
Se placer dans le répertoire principal.
```bash
sudo apt install nodejs
sudo apt install npm
npm install express
npm install socket.io
```

## Lancement

```bash
node ./server.js
```
